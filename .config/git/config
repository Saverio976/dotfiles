[user]
    email = xavier.mitault@epitech.eu
    name = Xavier Mitault
    signingkey = 8C56C3212DA73C7A635F0C6ED65697BC6225BA21
[init]
    defaultBranch = main
    ; templateDir = ~/source/SAMPLE_C_PROJECT
[push]
    autoSetupRemote = true
[pull]
    rebase = false
[commit]
    gpgsign = true
[gpg]
    program = gpg2
[alias]
    history = log --cherry --boundary --decorate --color --oneline --graph --all
    pretty = log --graph --pretty='%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset' --all
    quick-stats = !git-quick-stats
    branch-status = !git-branch-status
[core]
    editor = nvim
	; pager = diff-so-fancy | less --tabs=4 -RFX
[credential]
	helper = store
[interactive]
	; diffFilter = diff-so-fancy --patch
[color]
	ui = true
[color "diff-highlight"]
	; oldNormal = red bold
	; oldHighlight = red bold 52
	; newNormal = green bold
	; newHighlight = green bold 22
[color "diff"]
	; meta = 11
	; frag = magenta bold
	; func = 146 bold
	; commit = yellow bold
	; old = red bold
	; new = green bold
	; whitespace = red reverse
